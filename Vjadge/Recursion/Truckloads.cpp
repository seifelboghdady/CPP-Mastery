#include <bits/stdc++.h>
using namespace std;

int divide(int num, int load){
    if(num==load) return;
    return num = num/2;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int numCrat, loadSize; 
    while (cin>>numCrat>>loadSize)
    {
        // cout<<numCrat/loadSize+numCrat%loadSize<<endl;
    }
    

}