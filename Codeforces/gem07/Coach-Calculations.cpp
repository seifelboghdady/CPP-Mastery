#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int n,m; cin>>n>>m;
    vector<int> p(n+1);
    vector<int> q(m+1);
    for (int i = 0; i < n+1; i++)
    {
        cin>>p[i];
    }
    for (int i = 0; i < m+1; i++)
    {
        cin>>q[i];
    }
    //===================//

    

}