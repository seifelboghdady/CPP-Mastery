#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int h, a, b; cin>>h>>a>>b;
    if(h>=a && h<=b){
        cout<<"SIM";
    }else{
        cout<<"NAO";
    }

}