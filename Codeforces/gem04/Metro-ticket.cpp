#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int n,c,k; cin>>n>>c>>k;
    vector<int>days(n);
    for(int i =0; i<n; i++) cin>>days[i];
    //29
    // int formula = D*C+k;
    // vector<vector<int>> group(n);
    // for (int i = 0; i < ; i++)
    // {
    //     /* code */
    // }
    

}